# When true, the frontend uses in-memory fake data instead of calling the backend.
# Useful while the Flask side is still being built.
VITE_USE_MOCK=true

# When VITE_USE_MOCK=false, requests are proxied via /api (see vite.config.ts).
# No further config needed for local dev.
